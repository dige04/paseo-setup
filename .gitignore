# Dev-only, machine-specific paths (points at global pi install)
tsconfig.json

node_modules/
*.log
.DS_Store

# Host-local routing config and any credential-bearing local file. Real copies
# live in ~/.paseo-pi-team/ — these patterns only guard against accidental
# in-repo copies. `*.local.json` already covers model-routing.local.json and
# cluster-routing.local.json, so they are not listed separately.
*.local.json
auth.json

# Local-only notes, kept out of version control.
docs/*.local.md

# impeccable design-hook local state (cache is per-session; config is a
# reviewable waiver list — keep the config, drop the cache)
.impeccable/hook.cache.json

# Per-project skill install, regenerated by:
#   scripts/install.sh --skills-only --project .
# THIS repo is the source of those skills (skills/ is in the governed set), so
# committing the copy would double the governed bytes and invite drift between
# the two. Every OTHER project should commit its .claude/skills/ — a Paseo
# worktree is a separate checkout and does not inherit uncommitted files.
.claude/skills/
